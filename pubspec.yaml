name: stack_overflow
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: '>=3.3.2 <4.0.0'


dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.2+1
  bloc: ^8.1.4
  flutter_bloc: ^8.1.5
  intl: ^0.18.0
  date_field: ^5.0.1
  dartz: ^0.10.1
  retry: ^3.1.2
  connectivity_plus: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
